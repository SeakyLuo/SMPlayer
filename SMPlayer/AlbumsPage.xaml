<Page
    x:Class="SMPlayer.AlbumsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:data="using:SMPlayer.Models"
    xmlns:local="using:SMPlayer"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            x:Name="MusicLibraryTitleTextBlock"
            Style="{StaticResource TitleTextBlockStyle}"
            Text="Albums" />
        <ProgressRing
            x:Name="AlbumPageProgressRing"
            Grid.Row="1"
            Width="100"
            Height="100"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            IsActive="False"
            Visibility="Collapsed" />
        <GridView
            Grid.Row="1"
            Margin="10"
            IsItemClickEnabled="True"
            ItemsSource="{x:Bind Albums}"
            SelectionMode="None">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="data:AlbumView">
                    <UserControl>
                        <Grid
                            Width="180"
                            Height="240"
                            Margin="10"
                            PointerEntered="GridAlbumViewItem_PointerEntered"
                            PointerExited="GridAlbumViewItem_PointerExited"
                            Tapped="Grid_Tapped">
                            <Grid.ContextFlyout>
                                <MenuFlyout Opening="MenuFlyout_Opening" />
                            </Grid.ContextFlyout>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <controls:DropShadowPanel
                                x:Name="AlbumShadowPanel"
                                VerticalContentAlignment="Top"
                                BlurRadius="25"
                                OffsetX="5"
                                OffsetY="5"
                                ShadowOpacity="0"
                                Color="Gray">
                                <Image Source="{x:Bind Cover}" />
                            </controls:DropShadowPanel>
                            <TextBlock
                                Grid.Row="1"
                                Margin="0,5,0,3"
                                FontWeight="SemiBold"
                                MaxLines="2"
                                Text="{x:Bind Name}"
                                TextAlignment="Left"
                                TextWrapping="Wrap"
                                Visibility="{x:Bind Name, Converter={StaticResource VisibilityConverter}}" />
                            <TextBlock
                                Grid.Row="2"
                                FontSize="12"
                                Foreground="Gray"
                                Text="{x:Bind Artist}"
                                TextAlignment="Left" />
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="AlbumShadowPanel" Storyboard.TargetProperty="ShadowOpacity">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="0" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="AlbumShadowPanel" Storyboard.TargetProperty="ShadowOpacity">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="1" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Grid>
                    </UserControl>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
