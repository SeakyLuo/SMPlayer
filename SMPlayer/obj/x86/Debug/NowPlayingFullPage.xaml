<Page x:ConnectionId='1'
    x:Class="SMPlayer.NowPlayingFullPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:data="using:SMPlayer.Models"
    xmlns:local="using:SMPlayer"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:media="using:Microsoft.Toolkit.Uwp.UI.Media"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                        
    mc:Ignorable="d">

    <Grid>
        <Grid.Resources>
            <media:BackdropBlurBrush x:Key="AlbumBlurBrush" Amount="50" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border
            Grid.RowSpan="2"
            Background="{StaticResource AlbumBlurBrush}"
            BorderBrush="Black">
            <Image x:ConnectionId='5'
                x:Name="LargeAlbumCover"
                VerticalAlignment="Center"
                                                                                 
                Stretch="UniformToFill" />
        </Border>
        <local:MediaControl x:ConnectionId='2' x:Name="FullMediaControl" Mode="Full" />
        <controls:BladeView
            Grid.Row="1"
            BorderThickness="0"
            Background="{StaticResource AlbumBlurBrush}"
            BladeMode="Fullscreen">
            <controls:BladeView.ItemTemplate>
                <DataTemplate                        >
                    <Grid x:ConnectionId='3'>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:ConnectionId='4'                      />
                    </Grid>
                </DataTemplate>
            </controls:BladeView.ItemTemplate>
            <controls:BladeItem
                HorizontalAlignment="Stretch"
                Background="Transparent"
                TitleBarVisibility="Collapsed">
                <local:PlaylistControl />
            </controls:BladeItem>
        </controls:BladeView>
    </Grid>
</Page>

