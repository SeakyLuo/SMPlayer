<Page x:ConnectionId='1'
    x:Class="SMPlayer.NowPlayingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:data="using:SMPlayer.Models"
    xmlns:local="using:SMPlayer"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:media="using:Microsoft.Toolkit.Uwp.UI.Media"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                        
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock x:ConnectionId='2'
            x:Name="MusicLibraryTitleTextBlock"
            Style="{StaticResource TitleTextBlockStyle}"
            Text="Now Playing" />
        <CommandBar
            Grid.Row="1"
            Background="Transparent"
            DefaultLabelPosition="Right"
            IsOpen="False">
            <AppBarButton x:ConnectionId='4'
                x:Name="NewListButton"
                                           
                Icon="Add"
                Label="Add To"
                ToolTipService.ToolTip="Add To New Playlist" />
            <AppBarButton x:ConnectionId='5'
                x:Name="ClearButton"
                                         
                Icon="Clear"
                Label="Clear All"
                ToolTipService.ToolTip="Clear Now Playing" />
            <AppBarButton x:ConnectionId='6'
                x:Name="FullScreenButton"
                                              
                Icon="FullScreen"
                Label="Expand Page"
                ToolTipService.ToolTip="Full Window Display" />
        </CommandBar>
        <local:PlaylistControl x:ConnectionId='3'
            x:Name="NowPlayingPlaylistControl"
            Grid.Row="2"
            Margin="10"
            AlternatingRowColor="True"
            Theme="Light" />
    </Grid>
</Page>

